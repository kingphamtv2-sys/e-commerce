@extends('layouts.admin')
@section('title', __('admin.inventory.detail_title'))
@section('page-actions')<a href="{{ route('admin.inventory.adjust', $stock) }}" class="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-bold text-white">{{ __('admin.inventory.adjust') }}</a>@endsection
@section('content')
    @if(session('success'))<div class="mb-6 rounded-2xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-800">{{ session('success') }}</div>@endif
    <section class="mb-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div><h2 class="text-xl font-bold text-slate-950">{{ $productService->name($stock->product) }}</h2><p class="mt-1 text-sm text-slate-500">{{ $inventoryService->variantName($stock) }} · {{ $inventoryService->sku($stock) }}</p></div><span class="w-fit rounded-full bg-indigo-50 px-3 py-1.5 text-sm font-bold text-indigo-700">{{ __('admin.inventory.'.$stock->stockStatus()) }}</span></div><dl class="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div class="rounded-xl bg-slate-50 p-4"><dt class="text-sm text-slate-500">{{ __('admin.inventory.quantity') }}</dt><dd class="mt-1 text-2xl font-black">{{ $stock->quantity }}</dd></div><div class="rounded-xl bg-slate-50 p-4"><dt class="text-sm text-slate-500">{{ __('admin.inventory.reserved') }}</dt><dd class="mt-1 text-2xl font-black">{{ $stock->reserved_quantity }}</dd></div><div class="rounded-xl bg-indigo-50 p-4"><dt class="text-sm text-indigo-600">{{ __('admin.inventory.available') }}</dt><dd class="mt-1 text-2xl font-black text-indigo-700">{{ $stock->availableQuantity() }}</dd></div><div class="rounded-xl bg-slate-50 p-4"><dt class="text-sm text-slate-500">{{ __('admin.inventory.threshold') }}</dt><dd class="mt-1 text-2xl font-black">{{ $stock->low_stock_threshold }}</dd></div></dl></section>
    <section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="flex items-center justify-between border-b border-slate-200 bg-slate-50/70 px-6 py-5"><h2 class="font-bold text-slate-950">{{ __('admin.inventory.recent_logs') }}</h2><a href="{{ route('admin.inventory.logs', $stock) }}" class="text-sm font-bold text-indigo-700">{{ __('admin.inventory.view_all') }}</a></div>@include('admin.inventory._logs')</section>
@endsection
