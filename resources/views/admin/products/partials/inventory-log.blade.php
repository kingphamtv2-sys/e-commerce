<li class="flex flex-wrap items-center justify-between gap-2 rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm"><span class="font-bold text-slate-800">{{ __('admin.inventory.log_'.$log->type) }}</span><span class="text-slate-500">{{ $log->quantity_before }} → {{ $log->quantity_after }}</span><span class="text-xs text-slate-400">{{ $log->created_at?->format('d/m/Y H:i') }}</span></li>
