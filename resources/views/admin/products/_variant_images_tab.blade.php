<section x-show="activeTab === 'variant-images'" x-cloak data-product-tab="variant-images" x-data="{ selected: @js((string) ($variants->first()?->id ?? '')) }" @select-variant-images.window="selected = String($event.detail.id)" class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">
    <div class="border-b border-slate-200 bg-slate-50/70 px-6 py-5"><h2 class="font-bold text-slate-950">{{ __('admin.variant_images.title') }}</h2><p class="mt-1 text-sm text-slate-500">{{ __('admin.variant_images.description') }}</p></div>
    <div data-variant-images-empty @class(['p-10 text-center text-sm font-semibold text-slate-500', 'hidden' => $variants->isNotEmpty()])>{{ __('admin.product_tabs.variant_images_empty') }}</div>
    <div data-variant-images-manager @class(['hidden' => $variants->isEmpty()])><div class="border-b border-slate-200 p-6"><label class="text-sm font-bold text-slate-700">{{ __('admin.inventory.variant') }}</label><select data-variant-image-select x-model="selected" class="mt-2 block w-full max-w-xl rounded-xl border-slate-300 text-sm">@foreach($variants as $variant)<option value="{{ $variant->id }}">{{ $variant->name }} — {{ $variant->sku }}</option>@endforeach</select></div><div id="variant-image-panels">@foreach($variants as $variant) @include('admin.products.partials.variant-image-panel', ['variant' => $variant, 'variantImageService' => $variantImageService]) @endforeach</div></div>
</section>
